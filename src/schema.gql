# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Producto {
  pro_id: ID
  pro_nombre: String!
  pro_marca: String!
  pro_precio: Float!
  pro_talla: Int!
  pro_cantidad: Int!
  pro_estado: Boolean!
  pro_imagen: String
  pro_seccion: String!
}

type Talla {
  numero: Int!
}

type Persona {
  per_id: ID!
  per_nombre: String!
  per_apellido: String!
  per_sexo: String!
  per_telefono: String!
  per_email: String!
  per_direccion: String
  per_razon_social: String!
  per_tipo_persona: String!
  per_rol: String!
  per_tipo_identificacion: String!
  per_num_identificacion: String!
  per_estado: Boolean!
}

type Usuario {
  usu_id: ID!
  usu_nombre: String!
  usu_clave: String!
  usu_tipo_usuario: String!
  usu_estado: Boolean!
  persona: Persona
}

type Query {
  """Retorna todos los productos de estado TRUE"""
  AllProducts: [Producto!]!

  """Retorna un producto por su ID o NOMBRE."""
  ProductoByIDorName(term: String!): Producto!

  """Retorna todas las tallas disponibles."""
  AllTallas: [Talla!]!

  """Retornar todos los usuarios con su relación de Persona de estado TRUE."""
  AllUsuarios: [Usuario!]!
  findUsuarioByID(id: String!): Usuario!
}

type Mutation {
  NewProduct(createProductoInput: CreateProductoInput!): Producto!
  UpdateProduct(updateProductoInput: UpdateProductoInput!): Producto!

  """Actualiza el estado del producto a FALSE"""
  RemoveProduct(id: String!): Producto!
  createUsuario(createUsuarioInput: CreateUsuarioInput!): Usuario!
  updateUsuario(updateUsuarioInput: UpdateUsuarioInput!): Usuario!
}

input CreateProductoInput {
  pro_nombre: String!
  pro_marca: String!

  """El valor por defecto es 0."""
  pro_precio: Float = 0
  pro_talla: Int!
  pro_cantidad: Int = 0

  """El valor por defecto es TRUE."""
  pro_estado: Boolean = true
  pro_imagen: String
  pro_seccion: String!
}

input UpdateProductoInput {
  pro_nombre: String
  pro_marca: String

  """El valor por defecto es 0."""
  pro_precio: Float = 0
  pro_talla: Int
  pro_cantidad: Int = 0

  """El valor por defecto es TRUE."""
  pro_estado: Boolean = true
  pro_imagen: String
  pro_seccion: String
  _id: ID!
}

input CreateUsuarioInput {
  usu_nombre: String!
  usu_clave: String!
  usu_tipo_usuario: String!

  """El valor por defecto es TRUE."""
  usu_estado: Boolean = true
  persona: CreatePersonaInput!
}

input CreatePersonaInput {
  per_nombre: String!
  per_apellido: String!
  per_sexo: String!
  per_telefono: String!
  per_email: String!
  per_direccion: String
  per_razon_social: String!
  per_tipo_persona: String!
  per_rol: String!
  per_tipo_identificacion: String!
  per_num_identificacion: String!

  """El valor por defecto es TRUE."""
  per_estado: Boolean = true
}

input UpdateUsuarioInput {
  usu_nombre: String
  usu_clave: String
  usu_tipo_usuario: String

  """El valor por defecto es TRUE."""
  usu_estado: Boolean = true
  persona: UpdatePersonaInput
  _id: ID!
}

input UpdatePersonaInput {
  per_nombre: String
  per_apellido: String
  per_sexo: String
  per_telefono: String
  per_email: String
  per_direccion: String
  per_razon_social: String
  per_tipo_persona: String
  per_rol: String
  per_tipo_identificacion: String
  per_num_identificacion: String

  """El valor por defecto es TRUE."""
  per_estado: Boolean = true
  _id: ID
}